- To meet the conditions of a task involving an object that needs a state change (like temperature), efficiently use available environmental resources to process the object (such as using a microwave to heat) before executing the final placement.
